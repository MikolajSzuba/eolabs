# Data Input Folder

Place ENVI dataset pairs here (`.hdr` + `.bsq`).

Example:
- `scene_name.hdr`
- `scene_name.bsq`

This folder is intentionally kept lightweight in git. Large raw hyperspectral files should stay local.
